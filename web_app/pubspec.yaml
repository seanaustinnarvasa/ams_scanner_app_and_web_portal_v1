name: nustar_asset_scanner_app
description: NUSTAR Asset Management System Dashboard
publish_to: 'none'
version: 1.0.0+1

environment:
  # sdk: '>=3.2.0 <4.0.0'
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # cloud_firestore: ^4.15.0
  # firebase_core: ^2.24.0

  firebase_core: ^2.22.0
  cloud_firestore: ^4.13.0
  provider: ^6.0.5
  # google_fonts: 
  # provider: ^6.1.0
  # intl: ^0.19.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true